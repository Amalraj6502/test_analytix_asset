<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_renew_service_wizard_form" model="ir.ui.view">
        <field name="name">renew.service.wizard.form</field>
        <field name="model">renew.service.wizard</field>
        <field name="arch" type="xml">
            <form string="Renew Service Asset">
                <sheet>
                    <group>
                        <field name="asset_id" invisible="1"/>
                        <field name="reference_number_id" options="{'no_create': True, 'no_open': True}"/>
                        <field name="vendor_new_id"/>
                        <field name="vendor_bill_id"/>
                        <field name="purchase_price"/>
                        <field name="invoice_attachment_ids" widget="many2many_binary"
                                   options="{'filename':'name'}"/>
                        <field name="buy_date"/>
                        <field name="end_date"/>
                    </group>
                </sheet>
                <footer>
                    <button name="action_apply" string="Apply" type="object" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_renew_service_wizard" model="ir.actions.act_window">
        <field name="name">Renew Service</field>
        <field name="res_model">renew.service.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>
</odoo>
